import * as React from 'react'

import styles from './index.scss'

interface IProps {}

const ${ComponentName}: React.FC = (props: IProps) => {
    return <div className={styles.container}>${ComponentName}</div>
}

export default ${ComponentName}
